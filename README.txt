Google woff2 downloader for Ghost theme "Source"

Usage:

- cd to the root directory of the project
- edit your font definitions in fontdef.txt
- run ./google-fonts-downloader 
- run the generated ./partials/typography/*.hbs files with shell
- copy the *.hbs and *.woff2 files into the theme
- build the theme as usual
